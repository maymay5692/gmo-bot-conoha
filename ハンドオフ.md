# ハンドオフ (2026-04-19 更新)

## 現状サマリ

- **GMO bot v0.14.4** — 取引停止中、資金 13,060 JPY
- **Bitget FR 裁定**: Gate 1 FAIL 確定 (DSR=0, クリーン n=67)
- **MEXC FR monitor**: 稼働中、paper close 蓄積待ち (30+ close で Gate 1 検定)
- **セキュリティ事故**: 認証情報 GitHub 漏洩 → ADMIN_PASS rotate 済 (4/18)、残りは B-pre/B/C で整理予定

---

## ★次セッション最優先アクション (優先度順)

### P0: 緊急 or 意思決定待ち

#### P0-1. MEXC paper close 30+ 件到達後の Gate 1 検定

- 目安: 現在 open=3 で accrue 中、到達は **4/19-20 頃**
- 実行: `python3 scripts/dsr_check.py` もしくは `gate0_cvkelly.py`
- **重要**: DSR 計算に加えて **Protocol Incentive 込み Sharpe** も別途算出する
  - MEXC 手数料無料、MX 保有 50% 割引等をインセンティブとして加算
  - `concepts/protocol-incentive-arbitrage.md` のフレームワークを適用
- 判定分岐
  - PnL 単独で Gate 1 FAIL + Incentive 込みで PASS → MEXC 継続候補
  - 両方 FAIL → FR 撤退確定 → P1 ピボットへ

#### P0-2. セキュリティ事故の整理 (B-pre / B / C)

- 4/18 に A (ADMIN_PASS rotate) のみ完了、B-pre 以降は保留中
- 詳細: 本ファイル末尾「セキュリティ対応状況」参照
- 優先度は低いが、GitHub 履歴に古いパスワードが残っている状態は監査上好ましくない
- 判断: ユーザーと相談の上、段階的に処理

### P1: FR 撤退確定時のピボット候補 (準備作業)

#### P1-1. Hyperliquid エアドロ (第一候補)

- 根拠: 第 2 弾エアドロ確定 (38.888%)、HYPE 20 ドル換算で 80〜90 億ドル規模
- **Protocol Incentive Arbitrage** の代表例 — PnL 単独で負でも Sharpe 1→50 達成事例 (wiki)
- 着手手順 (加東たまお戦略)
  1. Arbitrum → ETH/USDC 準備
  2. Hyperliquid 入金
  3. HyperEVM Transfer
  4. エコシステム全体に分散参加 (TVL 高 + ポイント制プロトコル優先)
  5. **定期的に触る**（一回では不十分）
- Gate 2 Tail Safety に必ず「**インセンティブ喪失シナリオ**」「**カストディリスク**」を入れる
- 参考 wiki: `sources/hyperliquid-airdrop-strategy.md`, `hyperliquid-lazy-airdrop.md`, `lutwidse-hyperliquid-analysis.md`

#### P1-2. Backpack JP レンディング (risk-free baseline)

- 日本居住者に**唯一**レンディング機能を開放している取引所
- 戦略: BTC 担保 → SOL 借入 → ステーキング 6-7% → 実質利回り 4-5%
- $500 から着手可能、**負けない baseline** として HL と並行運用推奨
- **2028 年から分離課税 20%** になるので、それまでの出口戦略も要設計
- 参考 wiki: `sources/european-crypto-guide-2026.md`

#### P1-3. VPS volume-latency スキャルピング

- **実装済みコード・最適パラメータが wiki にある** (ぬけみち「ネタ放流」攻略)
- 構造: Binance/Bybit 出来高変化の MEXC 伝播ラグを収益化、200→9500 ドル実績
- 推奨 VPS: **Hyonix SG 4GB $12/月** (シンガポール、取引所近接)
- **注意**: 既に MEXC で FR monitor 稼働中。複垢運用すると MEXC 側の BAN リスクが同時顕在化する。P0-1 の結論待って判断
- パラメータ (wiki より): `NET_ENTRY=800, TAKE_PROFIT_PCT=0.001, STOP_LOSS_PCT=0.00045, MAX_HOLD_SEC=5.0, COOLDOWN_SEC=8.0, BURST_WINDOW_SEC=0.4`
- 参考 wiki: `sources/volume-latency-scalping.md`, `concepts/volume-latency-arbitrage.md`

### P2: インフラ・安全性改善 (バックログ)

#### P2-1. Tailscale による VPS アクセス路固め

- 動機: 4/18 の漏洩事故再発防止、ConoHa VNC のような脆い経路を迂回
- 構成: SSH/bot-manager Basic Auth を Tailscale 経由限定に
- 参考 wiki: `sources/tailscale-dev-environment.md`

#### P2-2. Claude Code Security 10 選の適用

- sandbox / deny / hooks / Managed Settings で認証情報流出を機構的に抑止
- 参考 wiki: `sources/claude-code-security-10.md`

#### P2-3. bot-manager の .env.local 挙動バグ修正

- `bot-manager/app.py` の import 順バグで `.env.local` 単独 rotate が効かない
- 4/18 の rotate は nssm 側も直接更新して対処した
- 修正: `from config import get_config` より前に `_load_env_file()` を呼ぶ
- B commit 時に含めると将来の rotate が楽

---

## モニター稼働状況 (4/18 02:32 単一化済み)

| モニター | 状態 | paper trade |
|---|---|---|
| Bitget (fr_monitor.py) | 稼働中 | 蓄積継続 (要 dedup) |
| MEXC (mexc_fr_monitor.py) | 稼働中 | **open=3、close 蓄積待ち** |
| Hyperliquid (hl_fr_monitor.py) | 稼働中 | 設計上 paper なし |

- 死活監視: `crontab */5` で `scripts/monitor_watchdog.sh` が自動再起動
- PID file lock + 強化 kill (SIGTERM→SIGKILL) で二重起動防止済み
- `python3 -u` で log flush 即時

### 確認コマンド

```bash
# レポート
python3 scripts/fr_monitor.py --report
python3 scripts/mexc_fr_monitor.py --report
python3 scripts/hl_fr_monitor.py --report

# プロセス
pgrep -fl "python3.*scripts/(fr|hl|mexc)"

# ログ
tail -5 scripts/data_cache/fr_monitor.log scripts/data_cache/hl_fr_monitor.log scripts/data_cache/mexc_fr_monitor.log
tail -10 scripts/data_cache/watchdog.log
```

---

## 検証結果サマリ (これまでの成果)

### Gate 1 検定 (Bitget paper, dedup 後)

| 指標 | 値 | 基準 | 判定 |
|---|---|---|---|
| Sharpe (trade) | 0.396〜0.435 | ≥ 0.5 | FAIL |
| PSR | 1.000 | ≥ 0.95 | PASS |
| **DSR (N=10)** | **0.000** | ≥ 0.95 | **FAIL** |
| Top-2 集中度 | 21〜46% | — | 脆弱 |

### 試行した改善案すべて FAIL

| 案 | 結果 | 理由 |
|---|---|---|
| A. Multi-exchange FR 合成 (FLAM) | 合成 Sharpe 0.526 → FAIL | ρ=0.43, FR はグローバル裁定で整列済 |
| B. Oracle Resolution (HL mark vs oracle) | net -872% → 決定的 FAIL | Mean reversion +17% vs fee 9bps/RT の 40 分の 1 |
| C. Gate 0 dedup + CV Kelly | DSR 0.000 のまま | Sharpe +0.067 の改善不足 |

### Bitget データ品質の経緯

- raw CLOSEs 累計 1763 件 — ほぼ全て phantom duplicate (watchdog kill regex バグで 337 プロセス並走)
- **真の clean 境界: 2026-04-18 02:32 UTC** (337 プロセス kill + 単一化実施時刻)
- それ以前は cluster dedup 必須

---

## 戦略評価フレームワーク (ナレッジ活用)

### 3-Gate Review + 4 Pitfall Checks

- **Gate 1 Accountability**: Sharpe ≥ 0.5, DSR ≥ 0.95
- **Gate 2 Tail Safety**: de-peg / 清算カスケード / **インセンティブ喪失** / **カストディリスク**
- **Gate 3 OOS 再現性**: IS/OOS split
- **4 Pitfall**: Regime / Execution / Leakage / Tail

### Protocol Incentive Arbitrage (重要)

`concepts/protocol-incentive-arbitrage.md` — **本プロジェクトの評価基盤を更新する概念**:

> 「PnL 単独で正？ No → Protocol Incentive 型の可能性大」
> 「Sharpe 1 → 50 への到達はインセンティブを含めないと説明不可」

**示唆**:
- Bitget FR Gate 1 FAIL は PnL 単独で測った結果。**インセンティブ込み評価は未実施**
- MEXC / HL で同じ計算を行うと別結論になる可能性
- Sharpe 算出時に「インセンティブ込み総リターン」を使うべき
- Gate 2 Tail Safety に「**インセンティブ喪失シナリオ**」を常に含める

### Funding Arbitrage のスケーリング天井

`concepts/funding-arbitrage.md` より:
- 資本集中によるカストディリスクが利回りを上回る閾値が存在する
- Bitget FR Gate 1 FAIL の構造的根拠 (既知のエッジ、リテール参入遅い)

---

## bot 現状

- **GMO bot v0.14.4** — 取引停止、metrics 収集のみ
- VPS <IP redacted>, Basic Auth credentials stored in `C:\gmo-bot\bot-manager\.env.local` (see 1Password)
- 資金 **13,060 JPY** (GMO、損益なし)
- Bitget / MEXC アカウントあり (未入金)
- **入金判断: STOP** (Gate 1 未通過、ピボット検討中)

---

## セキュリティ対応状況 (2026-04-18 インシデント)

### ✅ A. ADMIN_PASS rotate — 完了 (4/18)

- 旧パスワード (user email と同一) が GitHub の複数箇所で平文漏洩していた
- 新パスワードは 32 桁 hex、ユーザーが 1Password/紙メモで保管
- **旧パスワードは無効化確認済み** (curl で HTTP 401)
- 更新経路: `C:\gmo-bot\bot-manager\.env.local` + nssm AppEnvironmentExtra 双方
  - 理由: app.py の import 順バグで `.env.local` 単独では効かない (P2-3 で修正予定)

### ⏳ B-pre. 残りの漏洩箇所を redact — 未実施

検出済み漏洩 (旧 ADMIN_PASS 文字列を検索):

| ファイル | 行 | tracked | 対応 |
|---|---|---|---|
| ハンドオフ.md | 7, 132 (過去) | ✅ | ローカル redact 済 (未 commit) |
| docs/plans/2026-02-13-plan-d-implementation.md | 122, 256, 392 | ✅ | 未 redact |
| scripts/daily_healthcheck.py | 49 | ✅ | hardcoded fallback 削除要 |
| docs/superpowers/plans/2026-04-05-ghost-position-fix.md | 139, 147 | ❌ untracked | commit 前に要 redact |
| scripts/2026-04-08-002517-claude-bridgedsr.txt | 多数 | ❌ untracked | commit 対象外で放置可 |

### ⏳ B. 6 コミット分割 + GitHub push — 未実施

推奨コミット粒度:
1. `security: redact basic auth credentials from handoff and docs`
2. `feat: monitor lock mechanism to prevent duplicate runs`
3. `fix(mexc): exchangeInfo v3 status encoding + add paper trade logger`
4. `fix(bot-manager): load .env.local before Config import` ← P2-3 の修正
5. `docs(superpowers): add plans/specs for ghost-position, dvol, sl-pnl`
6. `feat(scripts): analysis and backtest scripts for D/F strategy evaluation`
7. `feat(scripts): Bitget/MEXC/HL funding rate monitors`

push 前必須: 旧 ADMIN_PASS 文字列と `git grep -nE "Basic Auth|ADMIN_PASS="` で残存漏洩ゼロ確認

### ⏳ C. git history rewrite + force-with-lease — 未実施 (ユーザー承認必須)

- `git filter-repo` で全履歴から漏洩行を削除
- force push は destructive action なので実行前にユーザー明示承認を取る
- **A (rotate) 完了後のみ意味あり** → 既に A 完了、前提条件充足
- 対象: ハンドオフ.md、docs/plans/2026-02-13-plan-d-implementation.md、scripts/daily_healthcheck.py

### 保留中 D. 運用ルール追加 proposal

- claude-bridge 側が `20260418-runtime-credential-handling` として起票予定
- app.py バグ情報も proposal に織り込み依頼済
- レポート: `~/Desktop/claude-bridge/inbox/2026-04-18-gmo-bot-credential-rotation-report.md`

---

## 作成・修正したスクリプト

| ファイル | 用途 |
|---|---|
| `scripts/_monitor_lock.py` | PID file lock (二重起動防止) |
| `scripts/monitor_watchdog.sh` | 死活監視 + 自動再起動 (cron 登録済み、4/18 regex 修正) |
| `scripts/dsr_check.py` | Gate 1 検定 (Sharpe/PSR/DSR + IS/OOS split) |
| `scripts/hedge_cost_recalc.py` | hedge cost 加算 P&L 再計算 |
| `scripts/fr_correlation.py` | 案 A: 3 取引所 FR 相関 + FLAM 合成 |
| `scripts/gate0_cvkelly.py` | 案 C: snapshot dedup + cluster dedup + CV Kelly |
| `scripts/oracle_resolution_backtest.py` | 案 B: HL mark vs oracle (no-lookahead) |
| `scripts/fr_monitor.py` | Bitget FR monitor + PID lock |
| `scripts/mexc_fr_monitor.py` | MEXC FR monitor + PaperTrader + hedge バグ修正済 (4/18) |
| `scripts/hl_fr_monitor.py` | Hyperliquid FR monitor + PID lock |
| `scripts/bitget_clean_dsr.py` | 4/16 16:11 UTC cutoff クリーン窓で Gate 1 再計算 |

---

## ナレッジ活用 (CCナレッジ wiki + claude-bridge)

### 現プロジェクトで参照中のページ

| wiki ページ | 使いどころ |
|---|---|
| `concepts/protocol-incentive-arbitrage.md` | **P0-1 の再評価フレーム** (最重要) |
| `concepts/funding-arbitrage.md` | FR 裁定の位置づけ、スケーリング天井 |
| `concepts/3-gate-review.md` / `4-pitfall-checks.md` | 全戦略の評価基準 |
| `concepts/edge-attribution.md` | Ilmanen 2 分類 + Protocol Incentive |
| `sources/hyperliquid-airdrop-strategy.md` | **P1-1 の行動計画** (加東たまお) |
| `sources/hyperliquid-lazy-airdrop.md` | P1-1 の最小手間 3 戦略 |
| `sources/lutwidse-hyperliquid-analysis.md` | P1-1 の Gate 2 Tail Safety 材料 |
| `sources/european-crypto-guide-2026.md` | **P1-2 (Backpack JP) の根拠** |
| `sources/volume-latency-scalping.md` | **P1-3 の実装済みコード・パラメータ** |
| `sources/vps-provider-evaluation.md` | P1-3 / P2-1 の VPS 選定 |
| `sources/tailscale-dev-environment.md` | **P2-1 の構成参考** |
| `sources/claude-code-security-10.md` | P2-2 の機構的対策 |
| `sources/hft-trader-edge-attribution.md` | Protocol Incentive 3 分類の出典、Sharpe 1→50 の事例 |
| `sources/hanguk-funding-arb-essay.md` | FR スケーリング天井、ポイント APR |
| `sources/mexc-fee-free-edge.md` | MEXC 手数料無料の扱い |

---

## 廃止リスト (再提案防止)

- Bitget 単独 FR 裁定のパラメータ追い込み (Gate 1 FAIL 確定)
- Oracle Resolution at 5-min sampling (fee > signal by 40x)
- FR multi-exchange 合成による Sharpe 改善 (ρ=0.43, 不十分)
- 検証なしでの入金
- パラメータ最適化でエッジを作ろうとすること
- **ハンドオフ.md や git 対象ファイルに認証情報を書くこと** (4/18 インシデント)

---

## Admin API

| 操作 | エンドポイント |
|---|---|
| ログ | `GET /api/logs?lines=N` |
| P&L | `GET /api/pnl/current` |
| Metrics CSV | `GET /api/metrics/csv?date=YYYY-MM-DD` |
| GMO 真値 | `GET /api/gmo/executions?date=YYYY-MM-DD` |
| デプロイ | `POST /api/admin/deploy` |
| Env 診断 | `GET /api/admin/env-status` |
| 自己更新 | `POST /api/admin/self-update {"restart": true}` |

Creds: see `C:\gmo-bot\bot-manager\.env.local` on VPS (or 1Password)

---

## 次セッション開始時のチェックリスト

1. MEXC paper close 件数の確認 (`python3 scripts/mexc_fr_monitor.py --report`)
2. 30+ close 到達なら Gate 1 検定 + Protocol Incentive 込み Sharpe 算出
3. Bitget monitor のデータ整合性 (cluster dedup)
4. セキュリティ対応 B-pre / B / C を進めるか再確認
5. FR 撤退確定なら P1 ピボット候補から着手順決定
