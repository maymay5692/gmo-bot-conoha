order_cancel_ms: 10000
order_interval_ms: 3000
execution_retain_ms: 30000
position_ratio: 0.9
min_lot: 0.001
max_lot: 0.001
max_position: 0.001
log_dir: "logs"
trade_log_enabled: true
metrics_log_enabled: true
alpha: 0.7
t_optimal_min_ms: 1000
t_optimal_max_ms: 10000
close_spread_factor: 0.4
stop_loss_jpy: 15.0
# v0.15.0 F案 (TP=1.0 + max_hold=300s + SL=-15):
#   min_hold_ms = 0 (open直後からclose試行)
#   max_hold_ms = 300000 (5分経過で市場成行close)
#   期待: 日次 +165 JPY (backtest実測、slippage込み)
min_hold_ms: 0
max_hold_ms: 300000
